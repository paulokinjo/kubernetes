# Vagrant - Kubernets 1 Master 2 Workers cluster setup

# Rancher
Installing docker
```
$ curl https://releases.rancher.com/install-docker/20.10.sh | sh

$ sudo groupadd docker

$ sudo usermod -aG docker $USER
```